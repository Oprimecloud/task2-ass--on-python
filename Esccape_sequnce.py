# file 4

# Ask for the name of a Nigerian dish and the state it is popular in. Print the result in two lines using `\n` and add a tab space before the state name using `\t`.
# Escape Sequence
dish_name = input("Kindly enter your Favourite Nigeria Dish: ")   # use input to collect user fav dish
state_dish_popular = input("Which State is the dish popular in ? ") # use input to collect their fav dish which state popular in
print(f"DISH NAME\tPOPULAR STATE NAME\n{dish_name}\t\t{state_dish_popular}") # then use print to print the result in two line with \n and \t to tab
